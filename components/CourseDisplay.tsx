import { Course } from '@/types'
import React from 'react'

interface CourseDisplayProps {
    course: Course;
  }

const CourseDisplay = ({course}: CourseDisplayProps) => {
  return (
    <div>
      
    </div>
  )
}

export default CourseDisplay
